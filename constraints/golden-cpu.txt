# STR-6 — the pinned set for the GOLDEN-GRADING environment.
#
# Deps in pyproject.toml are floors (`torch>=2.4`), which is right for a lab
# that runs on two very different boxes — but it left the determinism gate
# with no pin to bisect against when the golden reds: "did the physics
# change, or did numpy?" had no answer. This file is that answer.
#
# Source of truth: the EXACT versions the last green grading run resolved
# (CI run 32592441721, physics job, 2026-08-22 — transcribed from its
# "Successfully installed" lines, not from any local environment; the local
# boxes run CUDA/ROCm builds and deliberately stay on the floors).
#
# Used by the CI physics job via `pip install -c constraints/golden-cpu.txt`.
# When the golden reds after a dependency bump: bisect against THIS set
# first. Update the pins deliberately, in their own commit, with the golden
# re-graded green in the same change — never as a drive-by.
torch==2.13.0+cpu
numpy==2.4.6
matplotlib==3.11.1
pillow==12.3.0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.63.0
kiwisolver==1.5.0
pyparsing==3.3.2
python-dateutil==2.9.0.post0
six==1.17.0
packaging==26.3
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
pygments==2.21.0
