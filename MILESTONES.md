# Milestones — the lab's curriculum

Each milestone is one night's run, one new piece of behavior or one new
measurement. Mark a finished milestone `[x]` with a `(done <date> — <result>)`
note when the report it produced is committed into `reports/`.

---

## Phase 1 — verify

- [x] **M01** — 2D Ising verification. Reproduce Onsager's M(T) curve, locate T_c via susceptibility peak. (done 2026-06-08 — peak at T=2.30 ± 0.05, Onsager: 2.2692)
- [x] **M02** — Finite-size scaling: rerun at L = 32, 64, 128, 256, 512 and check that χ_max / L^(γ/ν) collapses (γ/ν = 7/4 for 2D Ising). (done 2026-06-15 — χ_max ∝ L^1.816 over L=32–256, R²=0.998; uses the |m|-susceptibility on the disordered side. L≥512 awaits a cluster updater — Metropolis critical slowing, see BACKLOG)
- [x] **M03** — Critical exponent β from data collapse of M·L^(β/ν) vs (T-T_c)·L^(1/ν). β/ν = 1/8 for 2D Ising. (done 2026-06-16 — β/ν = 0.131 over L=16–48 via Wolff cluster updates, residual 8.2e-3 vs exact 1/8; rescaled magnetization curves collapse onto one master curve. A short proof run gave 0.122; both bracket 1/8.)
- [x] **M04** — Specific heat curve C(T): the logarithmic divergence at T_c. (done 2026-06-24 — C-peak at T=2.275 vs Onsager exact 2.2692, rel. err 0.2%; χ-peak cross-check 2.280 from the same run, L=128, 25 temps in [2.0, 2.6], 40k sweeps, 33s on GPU. A second, *thermal* calibration of the same T_c the magnetization found in M01. The finite-L peak sits just above the infinite-volume value, as expected; the exact log amplitude A=(2/π)(2/T_c)²≈0.495 isn't resolved by a finite lattice, so the calibrated claim is the peak location, not the amplitude.)
- [x] **M05** — Verify lattice geometries beyond square: triangular (T_c = 4 / ln 3 ≈ 3.641), hexagonal (T_c = 2 / ln(2+√3) ≈ 1.519). Different geometries, same universality class. (done 2026-06-24 — triangular verified: χ-peak T_c(L=129) = 3.675 vs exact 4/ln3 = 3.6410, rel. err 0.9%; specific-heat peak independently at T_c = 3.650 from the same run, 25 temps in [3.3, 4.0], 40k sweeps, 63s on GPU. The triangular lattice is non-bipartite, so the square red/black checkerboard is physically wrong here — this needed a 3-sublattice update with color(i,j)=(i+2j)%3, which only wraps cleanly when 3|L, hence L=129. Same 2D Ising universality class as M01, a different exact T_c — a clean geometry check. The finite-L peak sits just above the infinite-volume value, as expected. Hexagonal (T_c = 2/ln(2+√3)) is the dual lattice — a natural next extension on the same engine; not yet run.)

## Phase 2 — map known territory

- [x] **M06** — 3D simple cubic Ising. Verify T_c ≈ 4.5115 (Monte Carlo benchmark). Different exponents (γ = 1.237, β = 0.326). (done 2026-06-16 — χ-peak T_c(L=12) = 4.504 vs MC benchmark 4.5115, rel. err 0.17%; specific-heat peak independently at T_c ≈ 4.42. CPU NumPy engine, 21 temps in [4.1, 4.9], 16k sweeps, 28s. Single-L Metropolis estimate — the small-L pseudo-critical peak carries an O(L^−1/ν) finite-size shift, so this is a calibration pass, not a precision T_c; an L-extrapolation would sharpen it. See BACKLOG.)
- [x] **M07** — Potts model (q-state). Run q = 3, 4, 5, 6. Phase transition is continuous for q ≤ 4 and first-order for q ≥ 5; show the qualitative change in your susceptibility curves. (done 2026-06-25 — all four exact T_c = 1/ln(1+√q) reproduced from the order-parameter χ peak on an L=64 square lattice, 25 temps per q in T_c±0.12, Wolff cluster updates: q=3 → 1.002 vs 0.995 (0.7%), q=4 → 0.907 vs 0.910 (0.3%), q=5 → 0.822 vs 0.852 (3.5%), q=6 → 0.749 vs 0.808 (7.2%); 471s on GPU. The continuous→first-order change is clear in the susceptibility curves: peak χ_max climbs monotonically 134 → 370 (continuous q≤4) → 824 → 1063 (first-order q≥5) — the taller, sharper spike of a discontinuous transition. Single-spin Metropolis is metastably trapped through this transition and reports noisy multi-peaked χ (a first attempt did exactly that); the **Wolff single-cluster updater** — Potts FK bond prob p=1−exp(−βJ), recolour the cluster to a random new flavour — fixes it, reusing wolff.py's cluster mechanics. q=5,6 are first-order with stronger finite-size/metastability shifts, so they carry a wider ±0.15 tolerance (vs ±0.1 for the continuous q≤4) — a physical allowance, not a fudge; both still land well inside it. The order parameter is the Potts m=(q·ρ_max−1)/(q−1).)
- [x] **M08** — 2D XY model. No long-range order at any T > 0, but a Berezinskii-Kosterlitz-Thouless transition at T_BKT ≈ 0.893. Measure the helicity modulus jump. (done 2026-06-25 — helicity-modulus jump crossing T_BKT(L=64) = 0.913 vs benchmark 0.8929, rel. err 2.2%; the spin stiffness Υ(T) crosses the Nelson-Kosterlitz universal-jump line Υ=(2/π)T at the transition, located by linear interpolation of the first downward root of Υ(T)−(2/π)T, 26 temps in [0.6, 1.1], 40k sweeps, 94s on GPU. This is the first model with **continuous** spins — angles θ∈[0,2π), E=−J·Σcos(θᵢ−θⱼ) — and the first transition with **no order parameter**: ⟨|m|⟩→0 at all T>0 (Mermin-Wagner), so the helicity modulus (free-energy response to a boundary twist), not a χ/C peak, is the signature. Estimator = the energy-like bond-cosine term minus the (β/N)-weighted *connected* variance of the bond current Σsin(θᵢ−θⱼ) — getting that 1/T factor and the connected form right is the #1 XY failure mode. Update: per-T-tuned-δ checkerboard Metropolis with interleaved microcanonical over-relaxation (the cure for XY critical slowing), which keeps Υ(T) smooth and monotone so the crossing is unambiguous; an embedded-Ising single-cluster Wolff (reflect across the line ⊥ a random axis — flipping the embedded ε, NOT reflecting across the axis itself) is available for the hardest near-T_BKT points. BKT has notoriously strong logarithmic finite-size corrections, so the single-L crossing sits a touch *above* the infinite-volume 0.8929 and the check carries a wider, physically-justified ±0.07 window — an L-extrapolation would sharpen it. The same single-L finite-size caveat as M05/M06 applies.)
- [x] **M09** — 2D Heisenberg (continuous spins). Confirm no phase transition in 2D (Mermin-Wagner). (done 2026-06-25 — the *absence* of order reproduced: at a fixed T=0.7 the per-spin vector magnetization ⟨|m|⟩ drifts 0.478 → 0.282 → 0.142 across L=16, 32, 64 (each step ×0.59, ×0.50 < 1; slope vs 1/L = +7.05 > 0, so ⟨|m|⟩ → 0 as L → ∞), 20k sweeps/L, 250s on GPU. This is the rare milestone whose *correct* answer is a NEGATIVE one — Mermin-Wagner forbids a 2D system with a continuous (here O(3)) symmetry from spontaneously ordering at any T > 0, and unlike the XY model (M08) the Heisenberg sphere S² is simply connected (π₁=0, no stable vortices), so there is no BKT escape either — no transition of any kind at finite T. The falsifiable signature is the finite-size drift of ⟨|m|⟩ toward 0; reading a single small L instead would fake an appreciable ⟨|m|⟩ and a spurious transition (the #1 way this ships wrong), so the run varies L and check_m09 asserts the monotone decrease + positive 1/L slope. The energy per spin is L-independent (≈ -1.15 at every size — an intensive quantity), the receipt that it's the order parameter washing out, not the lattices sitting at different states. New O(3) engine (heisenberg.py): 3-component unit-vector spins, E = −J·Σ S_i·S_j, **uniform-on-sphere** sampling (z=cosθ ∈ [−1,1] uniform, φ ∈ [0,2π) — NOT θ-uniform, which pole-biases and corrupts every energy/correlation), per-T-tuned-δ checkerboard Metropolis with interleaved microcanonical over-relaxation (reflect S about the local field — the cure for spin-wave critical slowing at the low-T, large-ξ points), plus an embedded-Ising single-cluster Wolff (reflect across the plane ⊥ a random axis — flipping the embedded ε, NOT reflecting through the axis itself, the recurring O(n)-cluster subtlety). A reproduced *known absence* earns the green leaf, distinct from an [~] failed calibration — the null IS the calibrated answer here.)
- [x] **M10** — Antiferromagnetic Ising on a bipartite lattice (same as ferromagnetic by sign-flip — sanity check the framework handles negative J cleanly). (done 2026-06-25 — staggered χ_s-peak T_N(L=128) = 2.282 vs Onsager exact 2.2692, rel. err 0.5%; specific-heat peak independently at 2.276 from the same run, 25 temps in [2.0, 2.6], 40k sweeps, 30s on GPU. J=−1 (antiferromagnetic) on the square lattice: by the bipartite sublattice gauge flip s_i→−s_i (on one colour) the AFM is *exactly* the ferromagnet in disguise, so the Néel point is Onsager's same T_c — the framework handles negative coupling cleanly. The order parameter is the **staggered** magnetization m_s = (1/N)Σ ε_i s_i with ε=(−1)^(x+y); its susceptibility χ_s peaks at T_N. The headline trap: the **uniform** ⟨|m|⟩ stays ≈0 at all T (≤0.003 across the whole sweep — the Néel state carries no net moment), so reading uniform m would show nothing and look broken. The square lattice is bipartite and unfrustrated, so the plain red/black checkerboard is exact — NO 3-colouring (that was M05's *frustrated* triangular problem; the frustrated triangular AFM is the later M13). Strongest correctness guard: an AFM↔FM gauge-duality cross-check test — the AFM's staggered observables equal an FM run's uniform observables bond-for-bond at the same |J|/seed (catches a silent ΔE sign-error that would secretly revert to the FM). The only code change from ising.py is the sign of J in ΔE; ising.py is untouched and its _neighbor_sum reused. The finite-L peak sits just above the infinite-volume value, as expected — the same finite-L peak caveat as M04 applies.)

## Phase 3 — push the edge (M14 done — the frontier crosses into Phase 4)

- [x] **M11** — Edwards-Anderson spin glass on a 2D square lattice. Run many disorder realizations, measure the disorder-averaged overlap distribution P(q) between two replicas. **In 2D the EA glass sits at the lower critical dimension — its spin-glass transition is at T_c = 0, so there is NO finite-temperature glass phase.** The verification is therefore the *approach to the T = 0 critical point*: P(q) broadens (its width / ⟨q²⟩ grows) monotonically as T → 0, without a finite-T transition. (The finite-T transition — T_SG ≈ 1.10 for ±J couplings, with a Binder-cumulant crossing — is the **3D** case — that's M12, not this. The original "develops structure at low T" phrasing was corrected to the T_c = 0 framing in the M11 PR.) (done 2026-06-25 — the broadening reproduced: the disorder-averaged ⟨q²⟩ grows monotonically 0.011 → 0.306 (×27, 15/15 temperature steps) as T falls 2.0 → 0.6 on an L=16 ±J lattice, 16 temps × 64 disorder realizations × 2 replicas in one batched GPU pass, 60k sweeps, 67s; the overlap stays symmetric (max|⟨q⟩| = 0.045 ≈ 0, P(q)=P(−q) residual 0.061), and the Binder cumulant rises smoothly 0.01 → 0.71 with no crossing — single-L, consistent with T_c = 0. This is the rare milestone (like M09's Mermin-Wagner null) whose *correct* answer is an expected behaviour, not a transition temperature — the known T=0-critical broadening reproduced earns the green leaf. The order parameter is the replica overlap q=(1/N)Σ s_i^α s_i^β between two replicas sharing the same quenched ±J bonds; the disorder average over many realizations is mandatory. Equilibration floor: single-spin checkerboard Metropolis cannot equilibrate the L=16 glass below T ≈ 0.5–0.6 (verified — even 4× burn-in leaves the coldest points in an under-equilibration *dip* where ⟨q²⟩ is suppressed, not enhanced; the textbook reason parallel tempering exists), so the trustworthy window starts at the floor T=0.6 and the broadening *trend toward T=0* is the claim, not the un-equilibrable T≲0.5 tail. Parallel tempering would reach colder — see BACKLOG/M12. The square lattice stays bipartite under random bonds (frustration is in the bond signs, not the geometry), so the red/black checkerboard update is still exact; the load-bearing bookkeeping is the J-weighted neighbour sum — left/up bonds come from the *rolled* coupling tensors — verified against a brute-force bond energy.)
- [~] **M12** — 3D EA spin glass — the harder, more famous case. Look for the spin-glass transition; for the bimodal ±J couplings this engine simulates, the literature benchmark is T_SG ≈ 1.102 (Hasenbusch–Pelissetto–Vicari 1.1019(29); Katzgraber–Körner–Young 1.120(4) — KKY's 0.951(9) is the Gaussian-disorder value). Disorder-averaged Binder cumulant crossing is the cleanest signature. (code merged 2026-07-01, PR #43 — the quick-CPU calibration ships a [~] null: at L=4,6,8 × 8 disorder realizations the multi-L Binder crossing resolves to T_SG≈0.56, so the finite-T crossing does not resolve at CPU scale — report reports/2026-07-04-m12.html. The GPU promotion run happened 2026-07-22 — L=6,8,10 × 200 realizations with parallel tempering, receipt reports/receipts/run-2026-07-22-m12.json — and returned a second null: crossing_T = 1.244 against the 0.95±0.15 band configured at the time. The 2026-08-01 audit found two defects that invalidate that run as a promotion gate. First, the PT swap scheduler derived swap parity from the sweep index, so the production default swap_every=10 pinned parity to 0 and odd-gap replica pairs were never attempted — the ladder decomposed into isolated 2-rung pairs, visible in the receipt as exactly 0.0 at every odd gap of swap_rate_by_L; PR #68 fixes this by counting swap rounds. Second, the 0.95 benchmark was the Gaussian-disorder value graded against a bimodal ±J simulation, so even a converged run near the correct ≈1.102 could not have passed; PR #68 corrects the benchmark and tolerance, with the checks.py mirror updated in its stacked follow-up. The 2026-07-01/04 CPU nulls ran swap_every=5 [odd] and are unaffected by the parity defect. Promotion to [x] now requires: [1] the parity fix and corrected benchmark merged, [2] a fresh multi-L GPU run whose crossing lands within the corrected tolerance, [3] human review of that run.)
- [x] **M13** — Triangular lattice antiferromagnet — frustrated, ordered ground states are degenerate. Measure the entropy by integration of C/T. (done 2026-07-04 — residual entropy reproduced: S0/N = 0.3338 k_B vs Wannier 0.3383, Δ=0.0045, by thermodynamic integration S0 = ln2 − ∫ C dlnT = ln2 − ∫(C/T)dT on the frustrated J=−1 triangular antiferromagnet, L=24, 40 geometric temps in 0.15–12.0; ground-state energy exact −1.0000/spin; check_m13 re-integrates C/T within its own ±0.03 tolerance — report reports/2026-07-04-m13.html, PR #44.) (The full L=96 run converges a few % lower toward ~0.32 from the finite-window integration systematic; the exact ground-state energy anchors it at every size and both bracket Wannier 0.3383.)
- [x] **M14** — Random-bond Ising (binary disorder p% antiferromagnetic bonds). Map the multicritical Nishimori point. (done 2026-07-05 — the exact Nishimori-line internal energy reproduced: the disorder-averaged energy per spin matches the exact identity E/N = −2·tanh(1/T) = −2(1−2p) across p ∈ [0.04, 0.16] on the Nishimori line (tanh(1/T)=1−2p) to max Δ = 0.015 (L=24, 64 ±J realizations, single-spin checkerboard Metropolis reusing M11's ±J bond machinery), 204s on CPU. This is a gauge-symmetry identity that holds at any lattice size — not a finite-size-shifted T_c — so it verifies cheaply and robustly; check_m14 re-derives −2·tanh(1/T) from each point's own T and re-grades the measured energy, a receipt not an echo. The multicritical Nishimori point itself (literature benchmark p_c ≈ 0.1094, T_c ≈ 0.9528) is mapped only APPROXIMATELY at this scale: the ferromagnetic ⟨|m|⟩ collapses 0.85 → 0.22 as p rises along the line, passing through ½ near p ≈ 0.126 and bracketing the benchmark, but the two-size (L=12,24) Binder-cumulant crossing does NOT resolve at reachable disorder averaging (the curves carry a large finite-size drift and do not cross in-window) — precise pinning of p_c is deferred to a large-L hero run. The green leaf is earned by the exact energy; the precise MNP stays a documented open edge. Report reports/2026-07-05-m14.html, PR — the lab's first result on a quenched-disorder phase diagram, and the first swept along an exactly-solvable line.)

## Phase 4 — genuinely open (we are here — M18 is the open bench)

- [x] **M15** — Glauber dynamics relaxation: quench from T=∞ to below T_c. Track domain growth — Allen-Cahn predicts L_domain(t) ~ t^(1/2). Measure the exponent. (measured 2026-07-07; machine check passed; reviewed 2026-08-02 — Ben's delegated batch approval, substance reviewed by Claude — the lab's first NON-equilibrium milestone. Measured coarsening exponent n = 0.486 ± 0.001 (stat), R² = 0.9999 from the correlation-length estimator (half-height of the equal-time G(r,t), computed by FFT/Wiener-Khinchin), with an independent energy-length cross-check L_e ∝ 1/(E−E_eq) giving n = 0.469; L=512, 48 random-quench seeds, single-spin Glauber / heat-bath dynamics [P(s→+1)=σ(2βh)], t up to 8000 sweeps quenched to T=1.498 ≈ 0.66·T_c, 34s on GPU. The domain length traces a clean power law over ~2.6 decades in t (L_c grows 4 → 73), and the effective exponent drifts UP to 0.494 on the late window — the textbook preasymptotic approach to the asymptotic Allen-Cahn ½. The measured exponent sits a few percent BELOW ½ — the well-documented finite-time correction (2D Ising coarsening approaches t^(1/2) *from below*). The defensible statement is n ≈ 0.47–0.49 with a systematic band of ±0.02 dominated by estimator/window choice — the ±0.001 OLS statistical error is misleadingly tiny (it only reflects how straight the log-log line is) and is deliberately NOT quoted as the uncertainty. #1 failure mode: reaching for a cluster (Wolff) updater — it flips whole domains at once and destroys the very coarsening being measured; M15 uses single-spin heat-bath only, and the x-axis IS real Monte-Carlo time. Second guard: a seed frozen into a metastable stripe (rare at 0.66·T_c) would saturate its L(t) — mitigated by averaging 48 seeds and excluding the finite-size-saturation tail from the fit window. `check_m15` re-selects the scaling window and re-fits the exponent from the saved L(t) (a receipt, not an echo). Report reports/2026-07-07-m15.html, PR.)
- [x] **M16** — Aging in spin glasses: after a quench, two-time correlator C(t, t_w) should depend on t/t_w only (aging) or only on t-t_w (equilibrium). Time-translation invariance breaks below the transition. (measured 2026-07-14; machine check passed; reviewed 2026-08-02 — Ben's delegated batch approval, substance reviewed by Claude. A 3D ±J Edwards–Anderson glass was quenched from T=∞ to T=0.60 and evolved with local single-spin Metropolis dynamics on L=12 across 64 independent disorder realizations. The saved C(t_w+Δt,t_w) table spans t_w=16,32,64,128 and Δt=8…256. Regrading the table gives a within-group RMS scatter for the aging variable Δt/t_w only **0.24×** the time-translation-invariant Δt scatter (5 shared ratio groups vs 6 shared lag groups); independently, at fixed Δt=64 the correlation rises **0.714→0.818** as t_w grows, ΔC=+0.104, so the older glass decorrelates more slowly. This is the expected breaking of time-translation invariance. **Boundary:** finite L/time and one low temperature establish an aging calibration, not a universal scaling function; cluster updates and parallel-tempering swaps were deliberately forbidden because they would corrupt the physical clock. `check_m16` re-derives both collapse residuals and the fixed-lag separation from the saved table.)
- [x] **M17** — KPZ growth on circular geometry — interfaces wrapping around a torus, GUE-Tracy-Widom distribution of fluctuations. Numerical exponents 1/3 in time, 2/3 in space. (measured 2026-07-22; machine check passed; reviewed 2026-08-02 — Ben's delegated batch approval, substance reviewed by Claude. The lab's first milestone that watches a **surface** rather than spins. A 1+1d interface was grown on a periodic ring under **single-step (corner) growth** — the textbook lattice KPZ model, equivalent to TASEP under the usual slope↔particle map — with sublattice-parallel updates that are *exact* here rather than approximate (same-parity sites are never adjacent, so no two corner flips can interfere; the same checkerboard argument that makes `ising.py`'s red/black sweep exact). Measured on L=4096 over 64 rings and 8,000 sweeps: **β = 0.3127** against the exact KPZ **1/3** (R²=0.9994 over 29 points spanning t∈[23, 8000], ~2.5 decades), **α = 0.4840** from the saturated width across L=16…192 against the exact **1/2**, giving **z = α/β = 1.548** vs the exact **3/2** and **1/z = 0.646** vs the exact **2/3** — the milestone's "2/3 in space", *derived* from the two measured exponents rather than assumed. **What makes the 1/3 a measurement rather than a fit:** the same width estimator and the same scaling-window rule were pointed at two more growth rules whose exponents are exactly known and *different* — Edwards–Wilkinson (KPZ minus the (∇h)² term) came out **0.2354** against its exact **1/4**, and random deposition **0.5003** against its exact **1/2**. Random deposition is the strongest anchor in the milestone because it is not an exponent but a **closed form**, w²(t) = p(1−p)t at every t with nothing fitted; the run tracks it to **0.6%**. A pipeline that manufactured β=1/3 out of any curve would have to report 1/3 three times, and `check_m17` refuses the run unless all three land on their *own* exact values and genuinely separate. **The distribution:** height fluctuations came out skewed **−0.1651** (droplet/curved) and **−0.2732** (flat) against the Tracy–Widom predictions −0.2241 (GUE) and −0.2935 (GOE) — each geometry landing nearer its *own* law by 2.2× and 2.4×. The **negative sign is the prediction, not a bug**: the local-minimum density is exactly q(1−q) (brute-forced in the tests), so v(u) = (p/2)(1−u²) and the KPZ nonlinearity λ = ∂²v/∂u² = −p < 0, which predicts the *mirrored* Tracy–Widom law. Skewness is the graded statistic because it is invariant under the affine rescaling χ=(h−v_∞t)/(Γt)^{1/3}, so neither model constant has to be fitted. **Boundary:** β sits 0.021 *below* the exact 1/3 — the documented preasymptotic approach from below, and the tell that this is the finite fit window rather than a defect is that EW misses its own exact 1/4 by 0.015 **in the same direction** while RD, which has no correlation time to be preasymptotic about, lands essentially exactly. The distributional claim is **class assignment**, not a full Tracy–Widom collapse: the droplet's third moment is the least-converged graded number in the run (gap 0.059, 1.9× its sampling sigma, against a ±0.06 band), and the report's boundary text is **derived** from the measured moment gaps rather than hand-written, so prose and data cannot drift apart. Kurtosis is carried as evidence and deliberately **not** graded. Report reports/2026-07-22-m17.html.)
- [ ] **M18** — Directed percolation in 2+1d at the absorbing-state transition. Universality class is one of the simplest non-equilibrium examples.

---

# The coherence book

The M ladder asks when a lattice of **spins** agrees. This one asks the same
question of **clocks**: N oscillators, each running at its own natural frequency,
each nudged toward the crowd. It is the second physics ladder in the lab, and it
follows the same rule as the first — **calibrate first** (reproduce a result that
is known exactly), then map, then push into the open.

Track K graduates the coherence research in `~/projects/coherence-lab/` into the
nightly instrument. That work already ran on the order-parameter susceptibility
χ(K) = N·Var(r); K01 is the rung underneath it, proving the same estimator finds
the one coupling in this model that theory pins exactly.

## Track K — coherence

- [x] **K01** — Kuramoto synchronization transition, calibrated. N coupled phase oscillators with Lorentzian natural frequencies; sweep the coupling K and locate the critical point against the exact mean-field `K_c = 2γ`. (done 2026-08-02 — K_c = 1.0007 vs exact 2γ = 1.0000, rel. err 0.07%; reviewed and approved by Ben 2026-08-02. The lab's first **non-lattice** milestone: no sites, no neighbours, no temperature — 2000 oscillators on a circle, coupled only through their own centroid `r·e^{iψ} = ⟨e^{iθ}⟩`, integrated with fixed-step RK4. The pair coupling collapses exactly to the mean-field form `K·r·sin(ψ−θ_i)`, which is what makes an N=2000 sweep an O(N)-per-step **NumPy CPU** job (≈105 s) rather than a GPU one. Sweeping 25 couplings over [0, 4γ] at γ=0.5, the susceptibility-style fluctuation **χ(K) = N·Var_t(r)** — the same estimator M04/M05/M06 peak-locate in temperature, refined with the same 3-point parabola — peaks at **K_c = 1.0007 against the exact 2γ = 1.0000** (rel. err 0.07%). **The stronger claim is not the peak but the curve:** above the transition the mean-field solution is a closed form with nothing fitted, `r(K) = √(1 − K_c/K)`, and the measured coherence matches it to **1.5 × 10⁻⁴** across 7 un-fitted couplings — the same role random deposition's exact `w²=p(1−p)t` plays in M17 and the Nishimori-line energy identity plays in M14. A pipeline that manufactured a plausible transition would still have to reproduce seven coherence values it never fitted. The **negative control** closes the other end: at K=0 the measured ⟨r⟩ = 0.0203 is nothing but the random-walk centroid of N scattered phases, 1/√N = 0.0224 — a collapsed frequency draw (all ω equal) would order at zero coupling and is caught before any peak is fitted. Natural frequencies are drawn by deterministic inverse-CDF **quantile** sampling on the grid (i+½)/N rather than from a generator, so the set is exactly antisymmetric (Σω = 0, no spurious ψ drift) and carries no sampling noise on top of the finite-size effect being measured; the Lorentzian's tails are **clipped** at |ω| ≤ 40γ, which — unlike truncating and renormalizing — leaves g(0) and therefore `K_c = 2/(πg(0))` exactly unchanged while keeping |ω|·dt ≪ 2π so no fast drifter is aliased into a spurious near-stationary lock. **Boundary:** the exact K_c = 2γ is an N → ∞ result and the sweep is a finite grid, so this is a calibrated finite-N estimate, not a precision measurement. Both systematics were *measured before* the tolerance was declared, not assumed: the estimate approaches 2γ **from above** — 1.0504 at N=250, 1.0256 at N=500, 1.0083 at N=1000, 1.0007 at N=2000 — and by N=2000 the run-to-run scatter from the initial condition (a second seed gave 0.9910) has overtaken the finite-N shift. The floor under both is the sweep's own resolution ΔK = γ/6 = 0.0833, which is what sets `check_k01`'s ±0.10 band — the *grid*, not the shipped run's luck. dt = 0.02 was cross-checked against dt = 0.01: the two agree on the graded peak to four decimals, so the number is a property of the model and not the integrator. The steepest-rise cross-check estimator is carried but deliberately **not** graded — `r(K) = √(1−K_c/K)` has infinite slope at K_c⁺, so a central difference on a finite grid can never place it better than ≈0.8·ΔK high, and it lands at 1.068 at every N and seed tried; that is a known property of the estimator, which is exactly why the fluctuation peak is the headline. Green-leaf review: Ben, 2026-08-02, same day as the machine pass.)
- [x] **K02** — The susceptibility's shape, and whether it survives N. Run 01 of the coherence-window work found the interior χ peak at partial order (r\*≈0.40) and fitted the closed form **χ(r) = a·r²(1−r)³** with an analytic interior maximum at r\* = 2/5 (R² = 0.967). K01 pins the *location* of that peak against exact theory; K02 asks whether its *shape* is real — re-derive the r²(1−r)³ form on this engine and test whether the fitted exponents hold as N grows, or whether they are a finite-size artifact of one population size. Source: `~/projects/coherence-lab/coherence-window/report/REPORT.md`. (measured 2026-08-02, **revised the same day after an adversarial literature assay** — machine check passed; green-leaf 2026-08-02 under Ben's pre-authorized "go to all" (post-revision), substance reviewed by Claude. **This milestone claims no novelty, and the revision is the point.** An assay against the Kuramoto finite-size-scaling literature (`docs/assays/2026-08-02-k02-literature-crosscheck.md`) found that essentially every element of K02 is published — in this model, with this frequency distribution, this sampling rule and this estimator, over a wider N range with far longer runs. This engine's deterministic inverse-CDF frequency draw is **Hong, Chaté, Tang & Park 2015 Eq. (4.1)**'s "regular" sampling; their §IV A treats the **regular Lorentzian** explicitly and its ω_j is this engine's frequency set term for term; χ = N·Var_t(r) is their estimator and **Park & Park 2024 Eq. (6)**. So K02 was re-scoped from "a finding" to what it honestly is: a **calibration** of this instrument against a published exponent, plus the refutation of a closed form only this lab ever proposed. **THE HEADLINE — a calibration, and it passes.** The coherence measured at the *exact* critical coupling decays with population size as **r(K_c, N) ~ N^−0.401 ± 0.017**, against the published **β/ν̄_c = 0.39(2)** for exactly this configuration (Hong et al. Eq. 4.3, N = 200…12800) — agreement at **0.42σ**. The ladder is 250 / 500 / 1000 / 2000 / 4000, twelve initial conditions per rung, ⟨r⟩ = 0.08423 / 0.06439 / 0.04875 / 0.03689 / 0.02772, R² = 0.99988. This needs no peak-finding at all: it evaluates at a coupling theory pins exactly, so there is no argmax to locate, no square-root Jacobian to fight and no fitted family to inherit assumptions from. **What the revision retired, and why.** The first pass reported the collapse as N^−0.28 from the free Beta fit's interior maximum p/(p+q). The assay correctly called that an estimator defect rather than a measurement: p/(p+q) is a ratio of two parameters that themselves track N (p: 0.50→0.91, q: 2.00→8.74) inside a family that demonstrably does not fit (free-fit R² only 0.73–0.82), so its N-dependence compounds real physics with the family's own misfit drift and the two cannot be separated afterward. Those numbers are still in the receipt, relabelled as a property of a misspecified fit; **−0.28 is not a scaling exponent and is not quoted as one anywhere.** Chasing the replacement then exposed a second, worse defect: the sweep's measurement window (t_burn 100, t_measure 200) is ample at N=250 and **badly insufficient at N=4000**. A traced run at K_c showed ⟨r⟩ still falling long after the sweep stopped looking — 0.070 → 0.049 → 0.036 → 0.028 → 0.027 across t ∈ [100,3000) — while N=250 sat flat from t=100. Reading the sweep's window at the top of the ladder measures a **transient**, inflating the coherence ~2.5× and giving a bogus 0.105. The shipped calibration therefore uses t_burn = t_measure = 2000, and every rung reports the drift between the halves of its own window so the check can *refuse* one still settling. **What survives unchanged.** χ(r) genuinely has an **interior** maximum at partial coherence at every rung, standing 2.2×–8.9× above both ends of the swept r-range. Run 01's form is decisively refuted: with the exponents **pinned to (2,3)** the fit scores a **negative R²** at every N (−3.39, −2.49, −2.35, −1.88, −1.56) — worse than a horizontal line through the data's own mean — and restricting to the clean ordered branch, where the form gets its most favourable footing, does not rescue it (−1.83 … −1.07). The measured argmax r\* sits ≥2.5σ below 2/5 at every rung. **The mechanism, restated more carefully than the first pass did.** χ peaks *at or near* the transition and the coherence there is r(K_c, N), which falls as a power of N — so r\* inherits that scaling and **cannot** be an N-independent constant; any single-N reading of the peak's r-coordinate, Run 01's 2/5 included, is measuring the population size. The first version said the peak sits at a **fixed** K_c at every N. That overstates the measurement: Hong et al. §III (Eq. 3.10) report the finite-N peak is always **subcritical** and only approaches K_c as N grows, drifting as δK_max ~ N^(−1/ν̄'), and this ladder's peak couplings scatter on both sides with std ≈0.024 — wider than the drift it would need to resolve. The honest statement is **unresolved from K_c at this resolution**; the mechanism survives because it only needs the peak to be asymptotically at K_c, which it is. **Negative controls.** Three. At K=0 the measured ⟨r⟩ is the 1/√N random-walk floor at every N. The random-vs-deterministic sampling distinction is the field's organizing axis — random draws put β/ν̄ at 0.20, a different universality class — so the calibration gate excludes 0.20 by 2.4 bands, catching a frequency draw that lost the quantile grid. And the assay's §4.1 flag was checked directly: the Lorentzian tail clip at |ω| ≤ 40γ is a deviation from the unclipped published configuration, so r(K_c) at N=1000 was re-measured at clip 100γ with dt reduced 4× to keep the fastest drifter resolved; the two agree at **1.1σ** (0.04977 vs 0.04744), so the clip is not carrying the exponent. **What the check gates:** the calibration against 0.39(2); equilibration at every rung, graded on the drift's **significance** rather than a bare percentage (a correction made after measuring — the 11.3% half-to-half change at N=2000 is only **1.52σ**, well inside the drift estimator's own noise, and a flat 10% constant would have refused a settled rung); an interior peak with margin; r\* more than its own floor from 2/5 **in either direction**, so a rung landing *on* 2/5 is inconclusive and fails too; and the K=0 control. The fitted (p,q), the demoted p/(p+q) trend and the χ-peak's position in K are **reported, never graded**. **Boundary:** the ladder tops out at N = 4000 = 2¹², deep in the pre-asymptotic regime where the effective exponent is ≈0.37–0.39; Park & Park 2024 Eq. (20) put the true asymptote at 0.325(15), reached only for N ≳ 2¹⁵, so this agrees with the literature *at this scale* and does not measure the asymptote. This is the deterministic model with clipped tails; Run 01's fit came from a **noisy** (D = 0.20) system at N = 24. **⚠ Run 01's r\* = 2/5 (a coherence) and β/ν̄ = 0.39 ≈ 2/5 (an exponent) are unrelated quantities that coincide numerically — nothing connects them.** Sources: Hong, Chaté, Tang & Park, *Phys. Rev. E* **92**, 022122 (2015), arXiv:1503.06393; Park & Park, *Phys. Rev. E* **110**, 034216 (2024), arXiv:2406.18904. Report reports/2026-08-02-k02.html.)
- [ ] **K03** — **Daido vs Hong: is the susceptibility exponent asymmetric across K_c?** chi ~ |K - K_c|^(-gamma) above the transition and ^(-gamma') below. Daido's perturbation theory (Prog. Theor. Phys. 75, 1460 (1986); J. Phys. A 20, L629 (1987); Prog. Theor. Phys. 81, 727 (1989); J. Stat. Phys. 60, 753 (1990)) predicts an **asymmetric** pair, gamma = 1/4 supercritical and gamma' = 1 subcritical, on the regular frequency distribution. Hong, Chate, Tang & Park, Phys. Rev. E 92, 022122 (2015) contradict it with **gamma = gamma' = 1/4** for the same class. Both statements are about the regular/deterministic-quantile Lorentzian, which the 2026-08-02 assay (sec 2.2) established is this engine's frequency set term for term — so the configuration class matches and the comparison is legitimate. This is a **live, checkable disagreement in the literature, in exactly K02's regime.** Measurement: a symmetric log-spaced grid in eps = |K - K_c|/K_c on BOTH branches, fitted separately. Feasible because nubar ~ 5/4 for this class (Hong sec IV A), so finite-size rounding sits at eps ~ N^(-4/5) = 0.0023 at N = 2000 and a grid starting at eps = 0.02 is a decade outside it. **K02's existing data cannot answer this** — its grid carries 12 dense points above K_c and only 4 below, so the branches are not comparably resolved; K03 needs its own two-sided run. Retargeted 2026-08-02 from the scheduled-coverage question (sources: `docs/assays/2026-08-02-k02-literature-crosscheck.md` sec 4.3; the earlier ALOHA target came from `~/projects/coherence-lab/coherence-window/report/RUN02-TAU.md`).

K02 shipped 2026-08-02 (runner, check, receipt, report — see its entry above).
K03 is **retargeted and not yet built** — no runner, no check, no receipt. The
scheduled-coverage/ALOHA question described above was K03's ORIGINAL target; the
2026-08-02 literature assay found a better one and the entry above has been
rewritten to it. The ALOHA question is not abandoned, it is unscheduled.

---

# The Citizen Science book

The physics ladder above proves the lab can be trusted. This book points that
trust *outward* — at real contributions a single patient machine can make to
science that other people use. Same rule as the physics: **calibrate first**
(reproduce a known result), then contribute, then submit to the official record.
See [CITIZEN_SCIENCE.md](CITIZEN_SCIENCE.md) for venues, contributor IDs, and the
provenance/DOI workflow. IDs are track-prefixed (C/A/I/B) and a verified
contribution carries its record as `{venue=… ; url=… ; doi=…}`.

## Track C — compute & number theory

- [x] **C01** — Calibrate the number stack: reproduce a known OEIS b-file segment byte-for-byte and re-verify a small known Mersenne prime. A pass means the arithmetic is trustworthy. (measured 2026-07-14; machine check passed; reviewed 2026-08-02 — Ben's delegated batch approval, substance reviewed by Claude. The runner generated the first **40** Fibonacci entries locally and matched their exact UTF-8 bytes against the official OEIS A000045 b-file prefix, then independently ran Lucas–Lehmer for M31=2^31−1 and reached final residue **0**. The receipt pins the retrieved 429,385-byte OEIS source by SHA-256 and retains the exact compared prefix; `check_c01` regenerates the bytes and reruns Lucas–Lehmer rather than trusting cached pass fields. **Boundary:** this calibrates integer arithmetic and source-byte handling; it is not a new OEIS extension, GIMPS assignment, or submitted contribution.)
- [ ] **C02** — Run one assigned GIMPS exponent (PRP + proof) to completion and submit the residue. {venue=GIMPS}
- [ ] **C03** — Extend an existing OEIS sequence's b-file by N verified terms (or submit a new sequence) and get it accepted. {venue=OEIS}
- [ ] **C04** — Join PrimeGrid and complete validated work units on an open prime problem (Proth / Sierpiński). {venue=PrimeGrid}

## Track A — astronomy from open archives

- [x] **A01** — Calibrate photometry: recover a *confirmed* TESS hot-Jupiter's period and transit depth from open MAST data, within the published error bars. (measured 2026-07-14; machine check passed; reviewed 2026-08-02 — Ben's delegated batch approval, substance reviewed by Claude. Eight official mission-produced TESS SPOC light curves for confirmed WASP-18 b / TIC 100100827 were downloaded from MAST and SHA-256 pinned. A dependency-free FITS reader selected quality-zero PDCSAP cadences; a blind Sector-2 transit search seeded an ephemeris, then the multi-year baseline across Sectors 2,3,29,30,69,96,103,104 refined **177** accepted transit timings without using the catalog value in the fit. Regrading those timings gives **P=0.94145236 d** vs NASA Exoplanet Archive **0.94145223±0.00000024 d** (Δ=0.00000013 d); the median local transit depth is **1.022%** vs **1.041±0.022%** (Δ=0.019%). Both fall inside the published error bars. `check_a01` refits the saved timings and depth samples. **Boundary:** calibration on public archive photometry, not a new planet detection, independent observation, or ExoFOP submission; raw FITS bytes remain at MAST and are pinned by URI/size/hash.)
- [ ] **A02** — Recover a known variable star's light curve and period; register an AAVSO observer code and submit one validated observation. {venue=AAVSO}
- [ ] **A03** — Reprocess one open LIGO/Virgo event from GWOSC and reproduce its published chirp mass within error. {venue=GWOSC}
- [ ] **A04** — Blind transit search across one TESS sector; vet the candidates and report a recovered planet. {venue=ExoFOP}

## Track I — the machine as instrument

- [~] **I01** — Calibrate the CMOS as a particle detector: cap the sensor, characterize dark noise, and separate hot pixels from track-like events (DECO/CRAYFIS method). (attempted 2026-07-14 — hardware null. This machine exposed no camera device and no real capped-sensor dark-frame stack was configured, so the runner refused to substitute synthetic frames or claim a measurement. The new real-stack path accepts `.npy`/`.npz` dark frames, removes frame-level drift, separates persistent hot pixels from transient ≥6σ connected components, and records elongated track-like candidates with per-file SHA-256 provenance; its synthetic fixtures test the classifier only and never enter the report. **Boundary:** even a future pass would calibrate dark noise/event separation, not identify cosmic rays without exposure metadata, controls, and a sustained rate/geometry study. Re-run `lab i01 --frames PATH` or set `WINDOWSILL_I01_FRAMES` when a capped sensor is available.)
- [ ] **I02** — Log cosmic-ray muon candidates for a month; check the rate against the ~1 / cm² / min expectation and the zenith-angle dependence. {venue=DECO}
- [ ] **I03** — Stand up a hardware-entropy beacon: publish a signed, timestamped randomness feed that passes the NIST SP 800-22 health tests. {venue=Zenodo}

## Track B — donate cycles (BOINC)

- [ ] **B01** — Join a team and complete validated work units on Einstein@Home (continuous-gravitational-wave / pulsar search). {venue=Einstein@Home}
- [ ] **B02** — Complete validated units on Rosetta@home or World Community Grid; archive the credit record. {venue=BOINC}

## Conventions

- Each milestone PR includes the report it generated (drop the HTML +
  JSON into `reports/`).
- Don't burn weeks of compute without checkpointing — every long run
  writes intermediate JSON every N samples.
- A milestone that doesn't reproduce a known result is a failed
  calibration, not a discovery. Verify before claiming. Mark it `- [~]`
  (instead of `- [x]`): it stays on the books as a recorded null and shows
  up as a folded grey leaf on the [seed](https://www.brokenbranch.dev/windowsill/),
  not a green one.
- Phase 4 results that LOOK novel get a second pass from a real physicist
  before sharing.
- The checkboxes here are the single source of truth for `lab publish`:
  `[x]` → human-promoted/verified, `[?]` → measured + machine-checked but
  awaiting review, `[~]` → failed or inconclusive calibration kept as a null,
  `[>]` → the explicitly-chosen open experiment (any track; add
  `{progress=0.4}` to show how far along it is — without a `[>]`, the first
  pending milestone is promoted to open automatically),
  `[ ]` → pending (the first pending is the open question on the bench).
