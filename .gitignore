__pycache__/
*.py[cod]
*.egg-info/
.venv/
.pytest_cache/
.coverage
build/
dist/
.playwright-cli/
output/playwright/

# generated by `lab setup` — the nightly job + its scheduler artifacts
scripts/nightly.sh
scripts/nightly.ps1
scripts/windowsill-lab.task.xml

# Scratch files from lab.atomic.atomic_write_text (tmp -> fsync -> replace) and
# from the hunt receipt writer in scripts/a05_hunt.py. A crashed write leaves one
# behind, and the nightly stages reports/ with `git add -A` — which picks up
# dotfiles. Nothing tracked here ends in .tmp.
.*.tmp
*.tmp
